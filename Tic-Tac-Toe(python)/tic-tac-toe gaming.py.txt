# tic_tac_toe_no_functions.py

board = [[  for _ in range(3)] for _ in range(3)]
current_player = X
game_over = False

print(Welcome to Tic-Tac-Toe!)
print(Board positions)
print(1  2  3)
print(---------)
print(4  5  6)
print(---------)
print(7  8  9)
print()

while not game_over
    # Print current board
    for row in board
        print(  .join(row))
        print(-  9)
    print()

    # Input move
    move = input(fPlayer {current_player}, enter your move (1-9) )
    if not move.isdigit() or not (1 = int(move) = 9)
        print(Invalid input. Enter a number from 1 to 9.n)
        continue

    move = int(move) - 1
    row = move  3
    col = move % 3

    if board[row][col] !=  
        print(That spot is taken. Try another one.n)
        continue

    board[row][col] = current_player

    # Check winner
    winner = False
    # Rows
    for r in range(3)
        if board[r][0] == board[r][1] == board[r][2] == current_player
            winner = True
    # Columns
    for c in range(3)
        if board[0][c] == board[1][c] == board[2][c] == current_player
            winner = True
    # Diagonals
    if board[0][0] == board[1][1] == board[2][2] == current_player
        winner = True
    if board[0][2] == board[1][1] == board[2][0] == current_player
        winner = True

    if winner
        for row in board
            print(  .join(row))
            print(-  9)
        print(f🎉 Player {current_player} wins!)
        game_over = True
        continue

    # Check draw
    is_draw = all(board[r][c] !=   for r in range(3) for c in range(3))
    if is_draw
        for row in board
            print(  .join(row))
            print(-  9)
        print(It's a draw!)
        game_over = True
        continue

    # Switch player
    current_player = O if current_player == X else X
